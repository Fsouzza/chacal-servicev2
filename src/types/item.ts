export type Item = {
  date: Date;
  lancamentos: string; 
  categoria: string;
  departamento: string;
  tipo: string;
  id: string;
  local: string;
  item: string;
  valor: number,
  obs: string;
}